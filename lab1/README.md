# Лабораторная работа №1. Вариант 7

## Статистическое оценивание числовых характеристик законов распределения случайных величин

---

## 📋 Содержание

1. [Теоретические основы](#теоретические-основы)
2. [Постановка задачи](#постановка-задачи)
3. [Установка и запуск](#установка-и-запуск)
4. [Методика выполнения](#методика-выполнения)
5. [Результаты экспериментов](#результаты-экспериментов)
6. [Анализ результатов](#анализ-результатов)
7. [Выводы](#выводы)

---

## 📚 Теоретические основы

### 1. Математическое ожидание и его оценка

**Математическое ожидание** (среднее значение) случайной величины X — это числовая характеристика, определяющая центр распределения вероятностей.

Для дискретной случайной величины:
```
E[X] = Σ xᵢ · P(X = xᵢ)
```

**Выборочное среднее** (оценка математического ожидания):
```
x̄ = (1/n) · Σ xᵢ
```

где:
- `n` — объем выборки
- `xᵢ` — i-е наблюдение

**Свойства выборочного среднего:**
- Несмещенность: E[x̄] = E[X]
- Состоятельность: x̄ → E[X] при n → ∞
- Эффективность: минимальная дисперсия среди всех несмещенных оценок

### 2. Среднеквадратичное отклонение (СКО)

**СКО** характеризует разброс значений случайной величины относительно математического ожидания.

**Несмещенная оценка СКО:**
```
s = √[(1/(n-1)) · Σ(xᵢ - x̄)²]
```

Делитель `(n-1)` вместо `n` обеспечивает несмещенность оценки (поправка Бесселя).

### 3. Доверительный интервал

**Доверительный интервал** — это интервал, который с заданной вероятностью β (доверительной вероятностью) содержит истинное значение параметра.

#### 3.1. Доверительный интервал для математического ожидания

При неизвестной дисперсии используется **распределение Стьюдента**:

```
P(x̄ - tβ · s/√n < μ < x̄ + tβ · s/√n) = β
```

где:
- `μ` — истинное математическое ожидание
- `tβ` — квантиль распределения Стьюдента уровня (1 + β)/2 с (n-1) степенями свободы
- `ε = tβ · s/√n` — полуширина доверительного интервала (погрешность оценки)

#### 3.2. Распределение Стьюдента

**Распределение Стьюдента** (t-распределение) используется когда:
- Выборка малого объема (n < 30)
- Дисперсия генеральной совокупности неизвестна

Свойства:
- Симметрично относительно нуля
- При n → ∞ сходится к стандартному нормальному распределению
- Имеет более «тяжелые хвосты», чем нормальное распределение

### 4. Отсеивание аномальных наблюдений

**Аномальные наблюдения (выбросы)** — это значения, существенно отличающиеся от основной массы данных.

**Метод отсеивания на основе прогнозного интервала:**
1. Вычисляется 95% **прогнозный интервал** для значений случайной величины
2. Наблюдения, выходящие за границы интервала, считаются аномальными
3. После удаления выбросов производится пересчет оценок

**Прогнозный интервал (Prediction Interval):**
```
[x̄ - t₀.₉₇₅ · s · √(1 + 1/n), x̄ + t₀.₉₇₅ · s · √(1 + 1/n)]
```

**Важное отличие:**
- **Доверительный интервал для среднего**: `t · s / √n` — показывает, где находится среднее значение μ
- **Прогнозный интервал**: `t · s · √(1 + 1/n)` — показывает, где может находиться следующее наблюдение

Для отсеивания выбросов используется именно прогнозный интервал, так как мы сравниваем **отдельные наблюдения** с ожидаемым диапазоном.

**Критерий:**
```
xᵢ < x̄ - t₀.₉₇₅ · s · √(1 + 1/n)  ИЛИ  xᵢ > x̄ + t₀.₉₇₅ · s · √(1 + 1/n)  =>  xᵢ — выброс
```

### 5. Связь доверительной вероятности и погрешности

Для заданной погрешности ε можно найти соответствующую доверительную вероятность:

```
β = 2 · F(t) - 1
```

где:
- `t = ε · √n / s`
- `F(t)` — функция распределения Стьюдента

---

## 🎯 Постановка задачи

На основе массива экспериментальных данных требуется:

1. **Найти оценку математического ожидания** по исходным данным
2. **Построить 95% доверительный интервал** для случайной величины
3. **Выполнить отсеивание аномальных наблюдений**, не попадающих в 95% интервал
4. **Найти уточненную оценку** математического ожидания после отсеивания
5. **Проверить качество оценивания**:
   - Построить доверительный интервал для заданной доверительной вероятности β = 0.89
   - Найти доверительную вероятность для заданной погрешности εβ = 0.28

### Исходные данные (Вариант 7)

```python
Массив данных: [0.5, 1.8, 3.3, 12.5, 6.9, 7.1, 6.2, 5.7, 4.4, 3.4, 1.6, 0.1]
Объем выборки: n = 12
Доверительная вероятность: β = 0.89
Максимальная вероятная погрешность: εβ = 0.28
```

---

## 🚀 Установка и запуск

### Требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)

### Установка

1. **Клонируйте репозиторий или перейдите в директорию проекта:**
   ```bash
   cd /path/to/lab1
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   python3 -m venv venv
   ```

3. **Активируйте виртуальное окружение:**

   - На Linux/macOS:
     ```bash
     source venv/bin/activate
     ```

   - На Windows:
     ```bash
     venv\Scripts\activate
     ```

4. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

### Запуск программы

```bash
python lab1.py
```

или

```bash
./venv/bin/python lab1.py
```

### Результаты работы

После запуска программа:
1. Выведет подробный текстовый отчет в консоль
2. Создаст файл `statistical_analysis_results.png` с графиками
3. Отобразит графики в отдельном окне (если доступен GUI)

---

## 🔬 Методика выполнения

### Шаг 1: Первоначальная оценка

Вычисляется выборочное среднее и СКО по всем данным:
```python
x̄ = np.mean(data)
s = np.std(data, ddof=1)
```

### Шаг 2: Построение 95% прогнозного интервала

Для отсеивания выбросов строится 95% прогнозный интервал для значений случайной величины:
```python
α = 0.05
t = stats.t.ppf(1 - α/2, n-1)
margin = t * s * np.sqrt(1 + 1/n)  # прогнозный интервал!
интервал = [x̄ - margin, x̄ + margin]
```

### Шаг 3: Фильтрация выбросов

Удаляются наблюдения, выходящие за границы 95% интервала:
```python
filtered_data = data[(data >= lower) & (data <= upper)]
```

### Шаг 4: Уточненная оценка

Пересчитываются оценки по отфильтрованным данным:
```python
x̄* = np.mean(filtered_data)
s* = np.std(filtered_data, ddof=1)
```

### Шаг 5: Доверительный интервал для β = 0.89

Строится доверительный интервал для математического ожидания:
```python
t_β = stats.t.ppf((1 + β)/2, n*-1)
ε = t_β · s* / √n*
интервал = [x̄* - ε, x̄* + ε]
```

### Шаг 6: Вычисление β для εβ = 0.28

Находится доверительная вероятность для заданной погрешности:
```python
t = εβ · √n* / s*
β = 2 · stats.t.cdf(t, n*-1) - 1
```

---

## 📊 Результаты экспериментов

### Первоначальные оценки

```
Оценка математического ожидания: x̄ = 4.4583
Оценка СКО: s = 3.4974
```

### 95% прогнозный интервал для отсеивания

```
Интервал: [-3.5537, 12.4704]
```

**Формула:** `[x̄ - t₀.₉₇₅ · s · √(1 + 1/n), x̄ + t₀.₉₇₅ · s · √(1 + 1/n)]`

### Обнаруженные выбросы

```
Количество выбросов: 1
Индексы: [3]
Значения: [12.5]
```

**Анализ:** Значение `12.5` выходит за верхнюю границу прогнозного интервала (12.47) и является статистическим выбросом.

### Уточненные оценки (после отсеивания)

```
Размер выборки: n* = 11
Уточненная оценка математического ожидания: x̄* = 3.7273
Уточненная оценка СКО: s* = 2.5299
Изменение оценки: Δx̄ = -0.7311
```

**Анализ:** После удаления выброса оценка математического ожидания уменьшилась на ~0.73, что составляет ~16.4% от первоначальной оценки. Это показывает значительное влияние выброса.

### Доверительный интервал для β = 0.89

```
Доверительный интервал: [2.3895, 5.0650]
Полуширина интервала: ε = 1.3378
```

**Интерпретация:** С вероятностью 89% истинное математическое ожидание находится в интервале [2.39, 5.07].

### Доверительная вероятность для εβ = 0.28

```
Вычисленная доверительная вероятность: β = 0.2788
```

**Интерпретация:** При погрешности 0.28 доверительная вероятность составляет всего ~28%, что является низким значением.

### Проверка качества оценивания

```
Требуемая доверительная вероятность: β = 0.89
Полученная погрешность: ε = 1.3378
Заданная максимальная погрешность: εβ = 0.28

Результат: ✗ КАЧЕСТВО НЕ УДОВЛЕТВОРИТЕЛЬНОЕ
Причина: Полученная погрешность (1.3378) существенно превышает заданную (0.28)
```

---

## 📈 Анализ результатов

### 1. Влияние выбросов на оценку

- **Выявлено:** 1 аномальное наблюдение (12.5)
- **Влияние:** Смещение оценки на 16.4%
- **Вывод:** Необходимость отсеивания выбросов подтверждена

Выброс `12.5` более чем в 3 раза превышает среднее значение после фильтрации (3.73) и серьезно искажает статистические характеристики выборки.

### 2. Точность оценивания

**Относительная погрешность:**
```
δ = ε / x̄* = 1.3378 / 3.7273 ≈ 35.9%
```

Относительная погрешность составляет ~36%, что является высоким значением для статистической оценки.

**Сравнение требуемой и фактической погрешности:**
```
Требуется: εβ = 0.28 (относительная ~7.5%)
Получено: ε = 1.3378 (относительная ~36%)
Превышение: в 4.8 раза
```

### 3. Доверительная вероятность

При заданной малой погрешности εβ = 0.28:
- Фактическая доверительная вероятность: β = 0.2788 (~28%)
- Требуемая вероятность: β = 0.89 (89%)

**Интерпретация:** Для достижения требуемой доверительной вероятности 89% при текущем объеме выборки погрешность должна быть существенно больше (~1.34).

### 4. Причины недостаточной точности

1. **Малый объем выборки:** n* = 11 (после отсеивания)
   - Погрешность пропорциональна `1/√n`
   - Для уменьшения погрешности в 4.8 раза требуется увеличить выборку в ~23 раза

2. **Высокая вариативность данных:**
   - Коэффициент вариации: CV = s*/x̄* = 2.53/3.73 ≈ 68%
   - Значения разбросаны от 0.1 до 7.1 (в 71 раз)

3. **Наличие выбросов в исходных данных**
   - Указывает на неоднородность процесса измерения
   - Требует дополнительного анализа источника данных

### 5. Необходимый объем выборки

Для достижения требуемой точности (ε = 0.28 при β = 0.89):

```
n_требуемое ≈ (t_β · s* / ε)²
n_требуемое ≈ (1.7247 · 2.5299 / 0.28)² ≈ 243 наблюдения
```

**Вывод:** Для обеспечения требуемой точности необходимо увеличить объем выборки примерно в 22 раза.

### 6. Визуальный анализ

На графиках видно:
- **Гистограмма:** Данные не имеют ярко выраженной симметрии, распределение скорее правостороннее
- **Боксплот:** После удаления выброса распределение стало более компактным
- **Доверительные интервалы:** 95% интервал для значений значительно шире, чем интервал для математического ожидания

---

## 💡 Выводы

### Основные результаты

1. **Успешно выполнено статистическое оценивание** математического ожидания случайной величины по экспериментальным данным варианта 7.

2. **Обнаружен и удален статистический выброс** (значение 12.5), который искажал оценку на ~16%.

3. **Уточненная оценка математического ожидания:** x̄* = 3.73 ± 1.34 (при β = 0.89)

4. **Качество оценивания признано неудовлетворительным:**
   - Полученная погрешность (1.34) в 4.8 раза превышает требуемую (0.28)
   - Для заданной малой погрешности доверительная вероятность составляет всего 28%

### Причины низкого качества

1. **Недостаточный объем выборки** (n = 11 после фильтрации)
2. **Высокая вариативность данных** (CV ≈ 68%)
3. **Наличие выбросов** в исходных данных

### Рекомендации по улучшению

1. **Увеличить объем выборки** до ~243 наблюдений для достижения требуемой точности

2. **Провести дополнительный анализ** источника данных для выявления причин выбросов

3. **Рассмотреть другие методы оценивания:**
   - Робастные оценки (медиана, усеченное среднее)
   - Методы, устойчивые к выбросам

4. **Проверить гипотезу о нормальности** распределения данных (критерии Шапиро-Уилка, Колмогорова-Смирнова)

5. **Исследовать возможность трансформации данных** (логарифмирование) для стабилизации дисперсии

### Практическая значимость

Выполненная работа демонстрирует:
- **Важность предварительной обработки данных** (отсеивание выбросов)
- **Критическую роль объема выборки** в точности статистических оценок
- **Необходимость проверки качества оценивания** перед принятием решений
- **Применение распределения Стьюдента** для малых выборок

### Теоретические выводы

1. **Метод доверительных интервалов** эффективен для оценки точности статистических оценок
2. **Распределение Стьюдента** корректно учитывает неопределенность при малых выборках
3. **Отсеивание выбросов** существенно улучшает качество оценок, но требует обоснования
4. **Связь β и ε** показывает компромисс между надежностью и точностью оценки

---

## 📁 Структура проекта

```
lab1/
├── lab1.py                               # Основной скрипт
├── requirements.txt                      # Зависимости
├── README.md                             # Документация
├── oed_1lab.pdf                         # Задание
├── venv/                                # Виртуальное окружение
└── statistical_analysis_results.png     # Результаты визуализации
```

---

## 📚 Использованные библиотеки

- **NumPy** — вычисления с массивами, статистические функции
- **SciPy** — распределение Стьюдента, статистические тесты
- **Matplotlib** — визуализация результатов

---

## 👨‍💻 Автор

Лабораторная работа №1, Вариант 7
Дисциплина: Обработка экспериментальных данных

---

## 📄 Лицензия

Образовательный проект

---

**Дата выполнения:** 2026-01-06
