# Лабораторная работа №4. Вариант 7. Часть 1

## Однофакторный регрессионный анализ

**Задача:** На основе массива экспериментальных данных построить уравнение регрессии в виде алгебраического полинома второй степени, проверить его адекватность и значимость коэффициентов регрессии. Расчёты произвести в скалярной и матричной форме.

---

## 📚 Теоретическая часть

### 1. Модель полиномиальной регрессии

Полиномиальная регрессия второй степени описывается уравнением:

```
y = b₀ + b₁·x + b₂·x²
```

где:
- `y` — зависимая переменная (отклик)
- `x` — независимая переменная (фактор)
- `b₀` — свободный член (intercept)
- `b₁` — коэффициент при линейном члене
- `b₂` — коэффициент при квадратичном члене

### 2. Метод наименьших квадратов (МНК)

Оценки коэффициентов находятся минимизацией суммы квадратов отклонений:

```
S = Σ(yᵢ - ŷᵢ)² → min
```

#### 2.1. Скалярная форма

Для построения системы нормальных уравнений необходимо вычислить следующие суммы:

```
Σ1, Σx, Σx², Σx³, Σx⁴, Σy, Σxy, Σx²y
```

Система нормальных уравнений имеет вид:

```
n·b₀ + (Σx)·b₁ + (Σx²)·b₂ = Σy
(Σx)·b₀ + (Σx²)·b₁ + (Σx³)·b₂ = Σxy
(Σx²)·b₀ + (Σx³)·b₁ + (Σx⁴)·b₂ = Σx²y
```

Решая эту систему относительно `b₀`, `b₁`, `b₂`, получаем оценки коэффициентов.

#### 2.2. Матричная форма

В матричной форме уравнение регрессии записывается как:

```
Y = X·B
```

где:
- `Y` — вектор наблюдений размера n×1
- `X` — матрица планирования размера n×p
- `B` — вектор коэффициентов размера p×1

Для полинома второй степени матрица планирования:

```
X = [1  x₁  x₁²]
    [1  x₂  x₂²]
    [⋮  ⋮   ⋮  ]
    [1  xₙ  xₙ²]
```

Оценка коэффициентов по МНК:

```
B = (X^T·X)^(-1)·X^T·Y
```

### 3. Оценка качества модели

#### 3.1. Коэффициент детерминации R²

Показывает долю вариации зависимой переменной, объясненной моделью:

```
R² = SS_reg / SS_tot = 1 - SS_res / SS_tot
```

где:
- `SS_tot = Σ(yᵢ - ȳ)²` — общая сумма квадратов
- `SS_reg = Σ(ŷᵢ - ȳ)²` — объясненная сумма квадратов
- `SS_res = Σ(yᵢ - ŷᵢ)²` — остаточная сумма квадратов
- `ȳ` — среднее значение y

**Интерпретация:** R² ∈ [0, 1]. Чем ближе к 1, тем лучше модель описывает данные.

### 4. Проверка адекватности модели (F-тест)

F-критерий Фишера проверяет гипотезу H₀: «модель статистически незначима».

```
F = MS_reg / MS_res
```

где:
- `MS_reg = SS_reg / df_reg` — средний квадрат регрессии
- `MS_res = SS_res / df_res` — средний квадрат остатков
- `df_reg = p - 1` — степени свободы регрессии (p — число коэффициентов)
- `df_res = n - p` — степени свободы остатков

**Критерий адекватности:**
- Если `F > F_crit(α, df_reg, df_res)`, то модель **адекватна** (отклоняем H₀)
- Иначе модель **не адекватна**

### 5. Проверка значимости коэффициентов (t-тест Стьюдента)

Для каждого коэффициента проверяется гипотеза H₀: `bᵢ = 0` (коэффициент незначим).

```
t_i = b_i / SE(b_i)
```

где `SE(b_i)` — стандартная ошибка коэффициента:

```
SE(b_i) = √(s²·(X^T·X)^(-1)_{ii})
s² = SS_res / df_res
```

**Критерий значимости:**
- Если `|tᵢ| > t_crit(α/2, df_res)`, то коэффициент **значим** (отклоняем H₀)
- Иначе коэффициент **незначим** и может быть исключен из модели

### 6. Алгоритм анализа

1. **Построение модели** (скалярная и матричная формы)
2. **Расчет расчетных значений** и остатков
3. **Вычисление R²** для оценки качества подгонки
4. **F-тест** для проверки адекватности модели
5. **t-тест** для проверки значимости каждого коэффициента
6. **Исключение незначимых коэффициентов** и повторная проверка адекватности
7. **Визуализация** результатов

---

## 📊 Анализ результатов

### Исходные данные (Вариант 7)

```
x = [-2,  0,  1,  2,  3]
y = [-2, -4,  1, 10, 21]
n = 5
α = 0.01 (уровень значимости)
```

### 1. Построение уравнения регрессии

#### Скалярная форма

Вычисленные суммы:
```
Σ1   = 5
Σx   = 4
Σx²  = 18
Σx³  = 36
Σx⁴  = 98
Σy   = 26
Σxy  = 79
Σx²y = 213
```

Система нормальных уравнений:
```
5·b₀ + 4·b₁ + 18·b₂ = 26
4·b₀ + 18·b₁ + 36·b₂ = 79
18·b₀ + 36·b₁ + 98·b₂ = 213
```

Решение:
```
b₀ = -3.257143
b₁ = 3.771429
b₂ = 1.985714
```

#### Матричная форма

Матрица планирования:
```
X = [1  -2   4]
    [1   0   0]
    [1   1   1]
    [1   2   4]
    [1   3   9]
```

Оценка по формуле `B = (X^T·X)^(-1)·X^T·Y`:
```
b₀ = -3.257143
b₁ = 3.771429
b₂ = 1.985714
```

**✓ Результаты совпадают!** (разница < 10⁻¹⁰)

### 2. Уравнение регрессии

```
y = -3.257143 + 3.771429·x + 1.985714·x²
```

### 3. Расчетные значения и остатки

| i | x    | y (эксп.) | y (расч.)  | Остаток    |
|---|------|-----------|------------|------------|
| 1 | -2.0 | -2.0      | -2.800000  | 0.800000   |
| 2 | 0.0  | -4.0      | -3.257143  | -0.742857  |
| 3 | 1.0  | 1.0       | 2.500000   | -1.500000  |
| 4 | 2.0  | 10.0      | 12.185714  | -2.185714  |
| 5 | 3.0  | 21.0      | 25.800000  | -4.800000  |

### 4. Качество подгонки

```
Среднее значение: ȳ = 5.2
SS_tot = 476.8 (общая вариация)
SS_reg = 447.542857 (объясненная моделью)
SS_res = 29.257143 (остаточная)

R² = 0.9386 (93.86%)
```

**Интерпретация:** Модель объясняет **93.86%** вариации данных — очень хорошее качество подгонки.

### 5. Проверка адекватности (F-тест)

```
n = 5, p = 3
df_reg = 2 (степени свободы регрессии)
df_res = 2 (степени свободы остатков)

MS_reg = 223.771429
MS_res = 14.628571

F = 15.301887
F_crit(0.01, 2, 2) = 99.000000
```

**Вывод:** `F (15.30) < F_crit (99.00)`
❌ **Модель НЕ АДЕКВАТНА** при α = 0.01 (очень строгий уровень значимости)

**Примечание:** При α = 0.05, F_crit = 19.00, и модель была бы адекватна. Это связано с малым объемом выборки (n=5) и очень строгим критерием (α=0.01).

### 6. Проверка значимости коэффициентов (t-тест)

```
s² = 14.628571 (дисперсия остатков)
s = 3.824722 (СКО остатков)

t_crit(0.005, 2) = 14.089051
```

| Коэфф. | Значение  | Ст. ошибка | t-стат.   | \|t\| > t_crit | Значим |
|--------|-----------|------------|-----------|----------------|--------|
| b₀     | -3.257143 | 2.421384   | -1.345165 | False          | НЕТ    |
| b₁     | 3.771429  | 1.701224   | 2.216660  | False          | НЕТ    |
| b₂     | 1.985714  | 0.365148   | 5.438324  | False          | НЕТ    |

**Вывод:** При α = 0.01 **все коэффициенты незначимы** (критерий очень строгий).

**P-значения:**
```
b₀: p = 0.310635
b₁: p = 0.158382
b₂: p = 0.032323
```

При менее строгом α = 0.05:
- b₂ был бы значим (p = 0.032 < 0.05)
- b₀ и b₁ остались бы незначимы

### 7. Визуализация

Программа создает 4 графика:

1. **График регрессии** — показывает соответствие модели данным
2. **График остатков** — проверка случайности отклонений
3. **Q-Q plot** — проверка нормальности распределения остатков
4. **Гистограмма остатков** — визуальная оценка распределения

---

## 💡 Выводы

### 1. Построение модели
- Успешно построена модель полиномиальной регрессии второй степени
- Результаты скалярной и матричной форм идентичны (проверка корректности)
- Уравнение: `y = -3.26 + 3.77·x + 1.99·x²`

### 2. Качество модели
- **R² = 0.9386** — модель объясняет 93.86% вариации данных
- Визуально модель хорошо описывает квадратичную зависимость
- Остатки распределены относительно равномерно

### 3. Статистические тесты при α = 0.01

#### F-тест (адекватность)
- **F = 15.30 < F_crit = 99.00** → модель **НЕ адекватна**
- Причина: очень малая выборка (n=5) + строгий уровень значимости
- При α = 0.05 модель была бы адекватна

#### t-тест (значимость коэффициентов)
- Все коэффициенты **незначимы** при α = 0.01
- Квадратичный член b₂ наиболее близок к значимости (p = 0.032)
- При α = 0.05 коэффициент b₂ был бы значим

### 4. Влияние размера выборки и уровня значимости

**Основные проблемы:**
1. **Малая выборка** (n=5):
   - Всего 2 степени свободы для остатков
   - Высокая критическая граница для F и t распределений
   - Низкая статистическая мощность тестов

2. **Строгий α = 0.01**:
   - Требует очень сильных доказательств для отклонения H₀
   - t_crit = 14.09 — нереалистично высокое значение для малых выборок
   - F_crit = 99.00 — чрезвычайно высокая планка

### 5. Практические рекомендации

**Для данной задачи:**
- Модель хорошо описывает данные (R² = 0.94)
- Квадратичная зависимость визуально очевидна
- Остатки не показывают систематических отклонений

**Для реальных исследований:**
1. **Увеличить объем выборки** до n ≥ 10-15 для полиномов 2-й степени
2. **Использовать α = 0.05** для баланса между строгостью и мощностью
3. **Дополнить статистические тесты** графическим анализом
4. **Рассмотреть альтернативные уровни значимости** для малых выборок

### 6. Методологические выводы

**Преимущества матричной формы:**
- Более компактная запись
- Легко обобщается на множественную регрессию
- Удобна для программной реализации
- Позволяет эффективно вычислять стандартные ошибки

**Ограничения классических тестов:**
- Критические значения сильно зависят от df
- Малые выборки требуют осторожной интерпретации
- Формальные тесты не заменяют содержательный анализ

---

## 🔧 Технические детали

### Запуск программы

```bash
cd lab4
python3 lab4.py
```

### Зависимости

```
numpy
scipy
matplotlib
```

Установка:
```bash
pip install -r requirements.txt
```

### Выходные файлы

1. `data_plot.png` — график исходных данных
2. `regression_analysis_results.png` — 4 графика анализа:
   - График регрессии с экспериментальными точками
   - График остатков
   - Q-Q plot для проверки нормальности
   - Гистограмма остатков

---

## 📖 Справочная информация

### Табличные значения

**F-распределение F(α, df1, df2):**
| α    | df1=2, df2=2 |
|------|--------------|
| 0.05 | 19.00        |
| 0.01 | 99.00        |

**t-распределение t(α/2, df):**
| α    | df=2         |
|------|--------------|
| 0.05 | 4.303        |
| 0.01 | 14.089       |

### Формулы

**Коэффициент детерминации:**
```
R² = 1 - SS_res/SS_tot
```

**Скорректированный R² (для множественной регрессии):**
```
R²_adj = 1 - (1 - R²)·(n-1)/(n-p)
```

**Стандартная ошибка регрессии:**
```
s = √(SS_res / df_res)
```

---

## 📚 Литература

1. Дрейпер Н., Смит Г. Прикладной регрессионный анализ. М.: Диалектика, 2007.
2. Кобзарь А.И. Прикладная математическая статистика. М.: Физматлит, 2006.
3. Montgomery D.C., Peck E.A., Vining G.G. Introduction to Linear Regression Analysis. Wiley, 2012.

---

**Автор:** Лабораторная работа выполнена в рамках курса "Основы экспериментальных данных"
**Вариант:** 7
**Дата:** 2026
